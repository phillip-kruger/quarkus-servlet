---
artifact: "${project.groupId}:${project.artifactId}:${project.version}"
name: "Servlet"
metadata:
  short-name: "servlet"
  keywords:
    - "servlet"
    - "http"
    - "jakarta"
  categories:
    - "web"
  status: "experimental"
  config:
    - "quarkus.servlet."
