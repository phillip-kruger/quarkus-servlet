<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee https://jakarta.ee/xml/ns/jakartaee/web-app_5_0.xsd"
         version="5.0">

    <!-- Never registered before: the deployment module did not read <listener> at all. -->
    <listener>
        <listener-class>io.quarkiverse.servlet.it.DescriptorListener</listener-class>
    </listener>

    <!-- Applied through a container-internal path; going through the spec-guarded
         ServletContext.setSessionTimeout instead used to fail the whole boot. -->
    <session-config>
        <session-timeout>42</session-timeout>
    </session-config>

    <!-- Grants what DescriptorSecuredServlet's annotation denies. The descriptor wins. -->
    <security-constraint>
        <web-resource-collection>
            <web-resource-name>descriptor-wins</web-resource-name>
            <url-pattern>/descriptor-wins</url-pattern>
        </web-resource-collection>
        <auth-constraint>
            <role-name>tester</role-name>
        </auth-constraint>
    </security-constraint>

    <login-config>
        <auth-method>BASIC</auth-method>
        <realm-name>integration-tests</realm-name>
    </login-config>

    <security-role>
        <role-name>tester</role-name>
    </security-role>
</web-app>
